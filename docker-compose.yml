# Local development setup. Mirrors kubernetes.yaml: same images, same
# environment variable names, but with host port mappings for easy debugging.
#
#   docker compose up --build -d
#
# Decision mode of the director service (simple | voting):
#   PowerShell:  $env:DECISION_MODE = "voting"; docker compose up -d director
#   bash:        DECISION_MODE=voting docker compose up -d director

services:

  # ------------------------------------------------------------ databases
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: iep-mysql-root
      MYSQL_DATABASE: authentication
    ports:
      # host port 3307 because a local mysqld often occupies 3306
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-piep-mysql-root"]
      interval: 5s
      timeout: 5s
      # first start initialises an empty data volume, which on a slow disk
      # takes well over a minute - start_period keeps those attempts from
      # counting as failures, so authentication is not skipped
      start_period: 180s
      retries: 30

  mongo:
    image: mongo:7
    ports:
      # host port 27018 because another local project may occupy 27017
      - "27018:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7
    # Append-only persistence so pending orders survive a restart.
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  # Ethereum simulator, used only when DECISION_MODE=voting.
  # -d gives deterministic accounts so tests know the private keys.
  ganache:
    image: trufflesuite/ganache-cli
    command: ["-d", "-p", "8545"]
    ports:
      - "8545:8545"

  # ------------------------------------------------------------- services
  authentication:
    build:
      context: ./authentication
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.10-slim}
    image: iep/authentication
    environment:
      DATABASE_HOST: mysql
      DATABASE_PORT: "3306"
      DATABASE_USERNAME: root
      DATABASE_PASSWORD: iep-mysql-root
      DATABASE_NAME: authentication
      JWT_SECRET_KEY: JWT_SECRET_DEV_KEY
      SERVICE_PORT: "5000"
    ports:
      - "5000:5000"
    depends_on:
      mysql:
        condition: service_healthy

  employee:
    build:
      context: ./employee
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.10-slim}
    image: iep/employee
    environment:
      MONGO_URI: mongodb://mongo:27017
      MONGO_DATABASE: fund
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      JWT_SECRET_KEY: JWT_SECRET_DEV_KEY
      SERVICE_PORT: "5001"
    ports:
      - "5001:5001"
    depends_on:
      - mongo
      - redis

  public-search:
    build:
      context: ./public_search
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.10-slim}
    image: iep/public-search
    environment:
      MONGO_URI: mongodb://mongo:27017
      MONGO_DATABASE: fund
      SERVICE_PORT: "5003"
    ports:
      - "5003:5003"
    depends_on:
      - mongo

  director:
    build:
      context: ./director
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.10-slim}
    image: iep/director
    environment:
      MONGO_URI: mongodb://mongo:27017
      MONGO_DATABASE: fund
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      JWT_SECRET_KEY: JWT_SECRET_DEV_KEY
      DECISION_MODE: ${DECISION_MODE:-simple}
      GANACHE_URL: http://ganache:8545
      SERVICE_PORT: "5002"
    ports:
      - "5002:5002"
    depends_on:
      - mongo
      - redis
      - ganache

volumes:
  mysql-data:
  mongo-data:
  redis-data:
