# Offline patch build - see director/Dockerfile.patch for the full explanation.
#
#   $env:DOCKER_BUILDKIT = "0"
#   docker build -f public_search/Dockerfile.patch -t iep/public-search:latest public_search
FROM iep/public-search:latest

COPY . /service/
